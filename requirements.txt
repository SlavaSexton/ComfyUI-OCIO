opencolorio>=2.2
opencv-python-headless
# OpenEXR is the EXR reader AND writer here. cv2 can do both, but only when OPENCV_IO_ENABLE_OPENEXR is set in
# the environment BEFORE cv2 is imported - which no ComfyUI launcher does (opencv/opencv#21326, the gate added
# after the 2022 EXR CVEs), and which this pack cannot fix from inside itself once another node pack has already
# imported cv2. Both halves were hit for real: a write returned without a file while Write still reported
# "saved name.0001.exr" (found by Andrei Orehov, PR #5), and a read raised a raw grfmt_exr.cpp traceback
# (reported as issue #4). OpenEXR needs no environment variable, and it is also the only way to write EXR
# header attributes at all.
# The floor is 3.3, not 3.2: that is where the OpenEXR.File API this pack calls in four places landed.
OpenEXR>=3.3
tifffile
Pillow
numpy
